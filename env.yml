name: zernikegrams
channels:
  - defaults
  - anaconda
  - conda-forge
  - salilab # dssp
dependencies:
  - dssp # or brew install brewsci/bio/dssp
  - boost=1.73.0 # pinned for dssp
  - pip:
    - . # pip install zernikegrams

